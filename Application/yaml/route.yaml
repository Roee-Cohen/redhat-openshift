kind: Route
apiVersion: route.openshift.io/v1
metadata:
# set your user name
  name:  user14-hello-openshift
spec:
  to:
    kind: Service
# set your user name
    name:  user14-hello-openshift
    weight: 100
  port:
    targetPort: 8080
  wildcardPolicy: None